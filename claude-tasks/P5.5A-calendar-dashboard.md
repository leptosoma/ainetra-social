# P5.5A — Calendar Workspace & Dashboard Simplification

Primary developer: Claude. Codex reviews and runs QA. Implement this task only. Do not commit; report files, schema, behavior, focused tests, limitations.

Read `CLAUDE.md`, `AGENTS.md`, `docs/product-bible.md`, relevant Phase 5.5 sections of `docs/roadmap.md`, the FullCalendar decision in `docs/oss-architecture-spike.md`, then only necessary calendar/dashboard, plan, capture, content, media and timezone files. Follow `AGENTS.md` by reading relevant local Next.js docs before code changes. Avoid a repository-wide scan.

## Deliverable

- Make `/calendar` the primary customer workspace. Use **FullCalendar Standard/MIT only**, with day, week, month and year views, route-scoped client code, Ainetra-owned event projection/status/action data, and no new calendar table. Desktop/tablet is the focus; do not build P5.5B mobile navigation/camera flows. Disable drag/drop if safe tenant/version/timezone/approval checks would expand this task; document why.
- Project only current non-superseded plan versions and `ACTIVE` items, plus relevant tenant-scoped scheduled posts. Respect `Business.timezone`, current content approvals/version, media availability, capture status and designed-creative authenticity. Expose plain Turkish labels for Ready/Media needed/Planned/Action needed/Ainetra can help; icon/text as well as color. Month cells concise; year overview only. Avoid raw enums.
- Clicking an event opens an accessible Ainetra drawer/sheet with correct date/platform/content/media need and only *working* context-relevant links/actions (`Medya ekle`, capture guidance if available, existing Fallback/Visual action if available, `İçeriği aç`). Do not fabricate an action, invent business facts, bypass sector/authenticity policy or mutate approval/schedule implicitly. Minimal existing-action routing is sufficient; document unconnected help flow.
- Dashboard top section states this week's active/current planned count, ready count, and user-action count and links to next real tasks. Use the same projection/count policy as calendar; make technical metrics secondary without removing useful capabilities. Light desktop navigation adjustment only if needed.
- Focused tests for stale-plan exclusion, tenant isolation, business timezone boundaries, status/label mapping, scheduled and media-needed details, cross-tenant event protection, designed creative not satisfying authentic requirement, approved-version safety, help routing/policy protection and dashboard counts. Test seams should verify real behavior, not merely mirror functions. Preserve Phase 1–5 behavior.

Scope exclusions: P5.5B mobile dock/camera redesign; publishing, OAuth, Postiz/Mixpost, BullMQ/tusd, analytics/learning, new AI engine or policy engine, unrelated refactors. No silent client-side authoritative reschedule. No premium FullCalendar.

Inspect first: `src/app/(app)/calendar/page.tsx`, `src/app/(app)/dashboard/page.tsx`, `src/features/content-stock/service.ts`, `src/features/content-planning/service.ts`, `src/features/publishing/service.ts`, `prisma/schema.prisma` relevant models, and only directly needed components/actions. Coordinate no edits to `TASKS.md`, `docs/current-state.md`, or `docs/roadmap.md`: Codex updates status after QA.
