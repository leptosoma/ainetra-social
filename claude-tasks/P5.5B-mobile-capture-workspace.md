# P5.5B — Mobile Navigation & Capture-First UX

Primary developer: Claude. Codex handles QA, docs, and final commit. Implement only this task; do not commit or push.

Read `CLAUDE.md`, `AGENTS.md`, `docs/product-bible.md`, the P5.5B section of `docs/roadmap.md`, `docs/current-state.md`, and the mobile decision in `docs/oss-architecture-spike.md`. Read relevant local Next.js docs as required by `AGENTS.md`. Inspect only protected layout/sidebar, dashboard/today, calendar, CaptureRequest, media upload, and related CSS/tests. Preserve P5.5A and prior domain behavior.

## Deliverable

- Mobile-only bottom dock around five actions: Bugün, Takvim, prominent capture button, İçerikler, Medya. Keep desktop sidebar. Group lesser-used links under an accessible İşletmem/Ayarlar entry without removing capabilities. Provide safe-area spacing, thumb-size targets, visible focus, and no content obscured by the dock.
- Accessible capture action sheet with photo camera, video camera/selection, gallery upload, and working `Ainetra ile içerik hazırla` route. Use native file inputs/capture hints with gallery fallback; no camera library. Send selected files through the existing authenticated `uploadMediaAction`/media service. Never trust client request/business metadata as authority; preserve server tenant/type/size/original/authenticity checks and current CaptureRequest fulfilment behavior.
- Mobile Bugün surface sourced from the existing business-timezone calendar projection and current OPEN CaptureRequests. Show simple today/next-action cards and a context-aware capture prompt when a current missing requirement exists. Use specific photo/video guidance and current media requirement tags; do not show stale/replaced/expired requests as current work. Route help only into existing content-plan/fallback/media/capture capabilities and do not imply a new engine or bypass confirmed-fact/sector/authenticity/version rules.
- Mobile calendar: light Ainetra-owned Today/3-day/Week/Month-overview presentation from existing projection; keep FullCalendar desktop/tablet only. Reuse server-side tenant/timezone/date semantics; no new calendar database or authoritative client schedule mutation. Month stays concise.
- If `npm run dev` is not already on port 3001 by default, set only its package script accordingly. Keep Control's 3000 untouched.

## Validation to add

Focused tests for current vs stale/other-tenant capture context; business-local today; context-specific media type/tag and existing upload/fulfilment contract; mobile calendar event selection/count and P5.5A regression; route/label safety and port default where appropriate. Test real seams, not implementation mirrors. Ensure sheet keyboard/focus/Escape behavior and responsive breakpoints can be manually checked. Do not add broad libraries just for tests.

No Phase 6, publishing, OAuth, native app, new AI engine, camera package, broad refactor, or unrelated environment changes. Leave `TASKS.md`, `docs/current-state.md`, and `docs/roadmap.md` for Codex after QA. Report files, behavior, tests, and limitations.
