# FIX — Desktop FullCalendar grid is blank

Status: READY; planning only. Claude is primary developer; Codex reviews/QA. Fix this regression **before P6-05**. Do not touch publishing behavior or redesign Calendar.

Observed at desktop width: Day/Week/Month/Year links and the server-rendered period change, but no FullCalendar grid appears below the toolbar, including an empty week/month. `page.tsx` renders the controls on the server; `calendar-grid.tsx` mounts `<DesktopCalendar>` only when `useMediaQuery("(min-width: 821px)")` reports true. P5.5B introduced that client-only gate; the view mapping and three Standard plugins are present, and no calendar source changed after P5.5B. **Hypothesis, not yet verified:** the desktop client branch is failing to mount or hydrate; a hidden/zero-height `.fc` grid or runtime error remains possible. The local app was not running during packet preparation, so no browser console result is claimed.

Inspect only `src/app/(app)/calendar/{page,calendar-grid,mobile-calendar}.tsx`, `src/features/calendar/`, `src/components/use-media-query.ts`, the calendar/responsive rules in `src/app/globals.css`, and direct P5.5 calendar tests. In a real desktop browser, check console/hydration errors, `matchMedia` result, presence of `.calendar-surface` and `.fc` DOM, computed display/height/overflow, plugin availability and the `day → timeGridDay`, `week → timeGridWeek`, `month → dayGridMonth`, `year → multiMonthYear` mapping. Distinguish "component never mounted" from "mounted but hidden" before changing code. Use the smallest fix and read applicable local Next.js docs before editing a Next file.

Browser regression coverage is required at **>820px**, using a known empty event fixture and at least one populated fixture: (1) Day renders `timeGridDay` time slots, (2) Week renders seven `timeGridWeek` columns, (3) Month renders `dayGridMonth` date cells, (4) Year renders `multiMonthYear` month/date cells, (5) zero events still render the grid rather than a blank area, (6) mobile at ≤820px retains its existing calendar and no desktop grid, and (7) no horizontal overflow at desktop and breakpoint widths. Check console for hydration/runtime errors. Keep existing P5.5 calendar tests green; add a focused browser check that asserts actual grid DOM/cells, not just selected links or period text. Report the verified cause and browser evidence.

No publishing files, feature expansion, new calendar design, or P6-05 work. Expected eventual fix commit: `fix: restore desktop calendar grid`.
